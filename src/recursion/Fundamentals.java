package recursion;

public class Fundamentals {
}

// Recursion ->

// 1) What?
// a) Recursion is used to do the same thing repetitively
// b) A function (recursive function) which calls itself

// 2) Why?
// a) In certain cases, it is much-much easier to write a recursive code rather using code.
// b) It helps us to utilise the leap of faith provided by recursive function
// c) Common Applications - trees, graphs, dynamic programming, stack, queue

// 3) How?
// a) Write a recursive function
// b) We need a base case (helps avoid StackOverflow error) -> base case always has a return statement
