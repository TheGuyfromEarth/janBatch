package linkedList;

public class Readme {

    // List interface in Collection Interface

    // ArrayList -> created using an array
    // LinkedList-> created using nodes

    // Node -> A combination of data and address of next node

    // Use the linked list if your list is having frequent insertions and deletions
    // Use the array list if your list requires frequent fetch operations
}
